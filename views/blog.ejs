<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("./components/head") %>
        <title>Sign in</title>
</head>
<body>
<%- include("./components/nav") %>

        <% if(locals.blog) { %>
                <div class="container mt-5">
                    <h1 class="card-title"><%= blog.title %></h1>
                    <img src="<%= blog.coverImgUrl %>" class="card-img-top" alt="...">
                    <pre class="card-body mt-5">
                        <%= blog.body %>
                    </pre>
                  </div>
        <% } %>


<%- include("./components/script") %>
</body>
</html>